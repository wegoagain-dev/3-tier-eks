# Kubernetes Secret Configuration for Kind Cluster
# Copy this file to .env.k8s and update with your values
# Then create the secret with: kubectl create secret generic database-secret --from-env-file=.env.k8s -n 3-tier-app-eks

DATABASE_URL=postgresql://postgres:password@postgres-db:5432/devops_learning
DB_PASSWORD=password
